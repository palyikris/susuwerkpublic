# switch ($x) {
#     condition {  }
#     Default {}
# }
# prompt: formula for switch

foreach ($currentItemName in $collection) {
    <# $currentItemName is the current item #>
    <# do something with $currentItemName #>
}
# prompt: formula for foreach